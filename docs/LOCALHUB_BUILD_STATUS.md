# LocalHub build status

> **Current-state supersession — 2026-09-02:** The evidence-backed visual pre-build and stability checkpoint is [localhub-current-state-report.md](./localhub-current-state-report.md). It supersedes this document's older claims that the production domain and Google OAuth lifecycle are incomplete. The authoritative hosted database still has 37 migrations; local migration 038 remains unapplied. This detailed document otherwise remains the historical Step 27/early-Step 28 record until the next full status reconciliation.

**Last updated:** 2026-08-31
**Branch:** `codex/localhub-production`
**Product:** LocalHub
**First configured market:** Lafia, Nasarawa State, Nigeria (market configuration, not the platform brand)
**READY TO SHIP:** No

## Verified checkpoint

The authoritative Supabase project is LocalHub, ref `exftgbfhmneweilsebjw`, in `eu-west-1`. The project identity is verified. `.env.local` is ignored, the Supabase binding uses the modern publishable key, and the configured OpenAI credential remains server-only; no Supabase service-role key is present.

Thirty-seven live migrations are applied:

| Version          | Name                                                  |
| ---------------- | ----------------------------------------------------- |
| `20260829163615` | `localhub_foundation`                                 |
| `20260829165428` | `localhub_runtime_hardening`                          |
| `20260829165912` | `localhub_security_and_index_hardening`               |
| `20260829170644` | `localhub_authenticated_policy_repair`                |
| `20260829172109` | `localhub_integrity_hardening`                        |
| `20260829172438` | `localhub_ledger_trigger_dispatch_fix`                |
| `20260829172915` | `localhub_onboarding_abuse_controls`                  |
| `20260829183305` | `localhub_onboarding_completion_validation`           |
| `20260829183314` | `localhub_admin_audit_reason_validation`              |
| `20260829183323` | `localhub_media_reservation_safety`                   |
| `20260829191626` | `localhub_admin_reason_whitespace_hardening`          |
| `20260829210257` | `localhub_public_route_slug_constraints`              |
| `20260829222846` | `localhub_listing_draft_workspace`                    |
| `20260829222856` | `localhub_draft_media_boundary`                       |
| `20260829223253` | `localhub_listing_draft_idempotency_index`            |
| `20260830083743` | `localhub_listing_draft_abuse_and_taxonomy_locks`     |
| `20260830102300` | `localhub_listing_customer_requests`                  |
| `20260830103713` | `localhub_market_integrity_trigger_dispatch`          |
| `20260830120433` | `localhub_listing_request_vendor_responses`           |
| `20260830130939` | `localhub_listing_request_vendor_response_rate_limit` |
| `20260830152835` | `localhub_order_snapshot_creation`                    |
| `20260830154455` | `localhub_order_rls_helper_hardening`                 |
| `20260830171813` | `localhub_order_vendor_transitions`                   |
| `20260830190627` | `localhub_order_fulfilment_processing`                |
| `20260830191055` | `localhub_order_fulfilment_fk_indexes`                |
| `20260830203751` | `localhub_admin_business_triage`                      |
| `20260830223544` | `localhub_finance_quarantine`                         |
| `20260830233802` | `localhub_payment_domain_dormant`                     |
| `20260831002742` | `localhub_provider_evidence_dormant`                  |
| `20260831014327` | `localhub_typed_ledger_dormant`                       |
| `20260831022710` | `localhub_referral_acquisition_links`                 |
| `20260831025734` | `localhub_referral_link_terminal_state`               |
| `20260831042504` | `localhub_legacy_search_demand_mission_quarantine`    |
| `20260831050224` | `localhub_demand_referral_mission_dormant`            |
| `20260831080111` | `localhub_notification_inbox_foundation`              |
| `20260831101256` | `localhub_notification_producers`                     |
| `20260831133056` | `localhub_unmet_demand_capture`                       |

The live database has 51 public plus 25 private tables; all 76 have RLS enabled. The six legacy search/demand/mission tables are quarantined behind forced RLS, zero policies/application ACLs, and always-failing mutation guards. The two private demand/referral-mission definition tables also force RLS, have zero policies/application privileges, and remain empty. Public notifications force RLS with exactly one active-self SELECT policy; five approved commerce boundaries now enqueue fixed in-app notifications transactionally, while the private zero-row delivery ledger forces RLS, has no policies or application privileges, remains outside Realtime, and is protected by always-enabled mutation blockers. Migration 037 adds one zero-row private market/category/day unmet-demand marker with forced RLS, zero policies/application privileges, and an authenticated-only non-oracular writer that independently revalidates active identity, canonical market/category, recognized timezone, and current zero public supply. Private inventory is 44 functions and public inventory is 78 functions. Realtime remains restricted to the sanitized public inbox; private finance/referral/demand/delivery objects are not published. Production catalog, commerce, B1/B2/B3, referral, finance, demand definitions/markers, mission, notification, notification-delivery, and audit rows remain zero by design. The order-number sequence is at the verified empty-project baseline `38`; rollback probes explicitly tolerate PostgreSQL's non-transactional sequence gaps and never rewind a shared sequence.

## Live security and correctness evidence

- Anonymous callers cannot invoke onboarding start/save/helper, media reservation, the protected admin queue, or legacy admin transition operations.
- Authenticated users can invoke onboarding start/save and media reservation only where internal authorization permits. The protected admin queue additionally requires an active `super_admin` capability and an explicitly selected active market. The legacy generic business-transition RPC is no longer executable by application or service roles. The onboarding helper is callable by neither application role.
- Terminal payload handling is strict; retries are idempotent; a shared market lock and business write-stability lock close the completion race.
- Category, contact, and whitespace validation is canonical. Administrative reasons reject control-whitespace, Unicode separators, and default-ignorable characters unless at least three meaningful alphanumeric characters remain; Yoruba, Hausa, and Arabic live probes remain valid.
- Database reservations expire after three hours; signed-token deadlines are two hours.
- Authenticated draft-only vendor listing workspace is verified: exact ALE 1.1 schema rendering/validation, RPC-only create/edit, idempotent create, optimistic revision/conflict handling, safe autosave/navigation with one reused history sentinel, and persisted draft-only media selection.
- Draft abuse/concurrency controls are verified: 30 creates per actor/24h, 240 edit saves per actor/hour, 50 open drafts/business, actor+key advisory serialization, and category→mapping→type→schema `FOR SHARE` locks.
- Cleanup is bounded with `SKIP LOCKED`: completed idempotency rows retain 30 days, incomplete/missing-result rows 24 hours, and rate rows 48 hours. The public cleanup RPC is service-role-only and must be scheduled daily; successful saves opportunistically prune 25 rows.
- Guest listing commitment is now a narrow capability flow: a service-role RPC derives the canonical live listing/business/market/category context, the browser receives only an HttpOnly/SameSite capability cookie, authentication claims rather than consumes the listing intent, and the request is materialized only after an explicit authenticated confirmation POST.
- Request creation is serialized and idempotent at the intent row, rate-limited per actor, validates the still-published authoritative listing, clears the capability hash on claim, and creates neither an order nor a payment. Customers receive scoped list/detail tracking, while accepted members receive a bounded vendor inbox. Customer/vendor RLS and the RPC authorization boundary passed live rollback-only cross-actor fixtures.
- Listing request responses use one canonical state machine: `open -> accepted | declined`. Only active accepted owner/manager/staff members of the exact active business can respond. A database-enforced fixed window permits at most 120 response attempts per active actor/hour before attacker-selected request lookup. Actor/key advisory locking, a request `FOR NO KEY UPDATE` lock, current-authorization replay checks, a private 30-day idempotency ledger, one minimized audit event, and an immutable transition trigger prevent races, replay leakage, duplicate effects, and direct app/service-role updates. Customer and vendor projections read the same request row; accessible UI feedback persists through the Next.js RSC refresh. The request-response operation writes no order, payment, fulfilment, notification, publication, match, or AI row.
- Listing-request cleanup is bounded with `SKIP LOCKED`: stale unconsumed intents retain 24 hours, consumed intents required by requests retain 30 days, and auth/actor limiter rows retain 48 hours. Its public cleanup RPC is service-role-only and must be included in the daily cleanup schedule.
- Vendor-response replay cleanup is separately bounded with `SKIP LOCKED` at 30 days; response rate-limit rows retain 48 hours and have their own bounded `SKIP LOCKED` cleanup. Both cleanup RPCs are exposed only to `service_role`. The live rollback probe exercises null decisions, stale/opposite outcomes, membership/profile/business revocation, exact GUC-forgery denial, the 120th/121st-call throttle boundary, rate-row pruning, private-boundary denial, nullable-FK intent pruning, projection isolation, excluded-workflow counts, and full fixture rollback.
- Migration 018 repairs the shared market-integrity trigger so PostgreSQL dispatches on the source table before binding table-specific `NEW` fields. A live rollback matrix passed valid insert/update and rejecting invariant cases for all 12 attached tables; no fixture data remained.
- Protected order creation is live but dormant. `listings.is_orderable` defaults to `false`, the migration activates no listing, and the live database has zero orderable listings. For a deliberately orderable live base listing, a 15-minute capability supports quantity 1–100; the server owns the listing, business, market, route, currency, price, and total truth. Placement re-locks and revalidates the active profile, claimed intent, quote, listing, category, business, and market before atomically writing one immutable `placed` order, one item snapshot, one canonical `placed` event, and one minimized audit event. Same-owner retries are idempotent; exact item/event cardinality, quote changes, variant activation races, direct app/service-role mutation, foreign actors, suspended profiles, and cross-business reads fail closed. Customer/vendor projections expose the same order without leaking buyer identity to vendor members. Payment, fulfilment, inventory, notification, and ledger work remain outside this placement slice; protected vendor transition evidence is recorded below.
- Order-intent creation, first placement, and replay use isolated fixed windows of 8/10 minutes, 20/hour, and 120/hour. Cleanup is bounded with `SKIP LOCKED`: stale unconsumed order intents retain 24 hours, consumed intents retain 30 days, and order limiter rows retain 48 hours. The service-role-only pruner and the complete order lifecycle/security fixture passed in a live rollback transaction after the permanent migration; all fixture and excluded-workflow counts returned to zero.
- Migration 022 moves the order child-table RLS authorization helper from the exposed `public` schema to `private`, updates both policies, and removes the public RPC/type surface. The helper remains boolean-only, fixed SQL, and executable only by `authenticated`; `anon` cannot execute it, and authenticated callers hold no `SELECT` grant on the private quote table. The permanent migration and complete post-apply rollback probe passed independent database, security, and code review with no P0–P2 finding.
- The protected vendor order transition is live: migration 023 permits only `placed -> confirmed | cancelled`, with exact active owner/manager/staff authorization for the order's business, actor-first 120/hour limiting, UUIDv4 idempotency, deterministic locks, immutable snapshots, exact terminal event/audit writes, synchronized customer/vendor reads, and no buyer-identity leakage. It performs no payment, fulfilment, inventory, notification, or ledger work. Terminal placement replay remains within the existing 30-day retained capability, cleanup preserves the order timestamp, and the post-apply rollback probe passed with zero residue. Structural concurrency review PASS; genuine two-session contention remains a low, nonblocking operational-test residual.
- Protected fulfilment processing is live but dormant. Migration 024 keeps the order at `confirmed` and creates a separate one-to-one `processing` aggregate only when an active accepted owner/manager/staff member explicitly starts handling the exact order. Actor-first 120/hour limiting, UUIDv4 idempotency, deterministic advisory/order locks, a same-order composite ledger FK, immutable aggregate/event/audit evidence, exact active-profile RLS, bounded customer/vendor projections, and service-role audit DML revocation passed rollback rehearsal, permanent apply, and the full post-apply adversarial probe. It writes no payment, inventory, delivery, notification, ledger, or extra order-status event. Migration 025 added exact composite-FK covering indexes and cleared both new unindexed-FK advisories. Customer and vendor processing state stays synchronized without buyer, event-actor, or payload leakage. Genuine two-session contention, scheduled cleanup, and the actor-deletion FK path remain documented P3 operational-test residuals.
- The first protected admin command-centre slice is live and read-only. Migration 026 exposes only an eight-column, PII-minimized pending-business triage projection to an active `super_admin` for one explicitly selected active market, with strict keyset pagination and no decision, contact, publication, payment, or customer action. It also revokes the legacy global business-transition RPC from `public`, `anon`, `authenticated`, and `service_role`; removes service-role execution from profile suspension; revokes direct `admin_events` DML and adds a statement-level immutable trigger. The migration repairs the pre-existing onboarding metadata validator by replacing a nonexistent `pg_catalog.jsonb_object_length` call with a bounded `jsonb_object_keys` count while preserving its validation rules. Combined rollback rehearsal, permanent apply, full post-apply probe, generated types, and zero-residue counts pass. The global pending-queue index remains a P2 performance watch until representative-scale `EXPLAIN` evidence is available.
- Step 23 Slice A is live and dormant. Migration 027 asserts all seven finance/ledger/referral tables are empty, removes every effective application table/column privilege from them, closes generic journal/helper RPC execution, changes the payment-event/payment FK to `ON DELETE RESTRICT`, and adds a private append-only payment-event trigger. It creates no provider, payment, account, journal, grant, transaction, refund, payout, or reconciliation capability. The rollback rehearsal, permanent apply, complete post-apply probe, zero-residue verification, and Sol finance/security review pass. Genuine two-physical-session contention remains an activation-blocking P2 verification gap because the available MCP execution serialized the attempted probe; it must pass before any finance activation.
- Step 23 Slice B1 is live and dormant. Migration 028 adds exactly three private tables—canonical order-payment state, payment attempts, and payment applications—and four private enums. The tables are empty, force RLS, have zero policies and zero application grants, and use owner-level row/truncate guards. The migration creates no provider evidence, journal/account taxonomy, route, callable command, finance grant, transaction, refund, payout, or reconciliation capability. Rollback rehearsal, permanent apply, and the complete post-apply probe pass with zero residue. Database and mandatory code reviews pass with no P0/P1/current-slice P2; Sol reports SAFE, retaining only activation-time two-physical-session contention and future behavioral-negative-test requirements.
- Step 23 Slice B2 is live and dormant. Migration 029 adds only three empty private objects: a signed-delivery envelope, separately purgeable exact `bytea` payload, and minimized normalized provider event, plus one enum and one private mutation guard. Exact SHA-256/length checks, exact and semantic dedupe, attempt binding/quarantine, forced RLS, zero policies/application grants, and owner row/truncate blockers are verified. It creates no route, provider credential, writer, payment/order mutation, journal/account, transaction, refund, payout, or reconciliation capability. The post-apply adversarial probe passes with zero B1/B2 rows. Database review has no P0/P1/P2; Sol has no dormant P0/P1/P2; mandatory code review PASS.
- Step 23 Slice B3 is live and dormant. Migration 030 adds three empty private tables—`commerce_ledger_accounts`, `commerce_ledger_journals`, and `commerce_ledger_posting_pairs`—five fixed private enums, one invoker fixed-path mutation blocker, and two additive exact source identities. It enforces exact account-purpose shapes; source amount/currency binding only to matched provider evidence/application; unique posting keys; positive JS-safe money; kind-bound debit/credit purposes; inherent equal pairs; exact one-time reversal account swap; self-reversal denial; and restrict-delete indexed FKs. It adds no mutable balance/JSON, seed, writer, command, route, provider credential, payment/order mutation, transaction, refund, payout, or reconciliation. RED 7/7 failed for expected missing files; final focused B3 7/7 and predecessor 4 files/29 tests pass. Two independent final reviews pass with no P0/P1/P2; the optional P3 exact legacy payment-event FK binding was closed before apply. Two rollback rehearsals, permanent apply, expanded adversarial probe, and independent zero-residue checks pass.
- Step 25 Slice A1 is live and deliberately non-financial. Migration 031 quarantines the four empty legacy referral tables behind force RLS, zero policies/grants, and row/truncate blockers, then adds only a private referrer identity and one per-owner/market merchant-onboarding link. Five fixed-path, PostgreSQL-owned RPCs provide active-profile identity/link ownership, strict enable/disable controls, and a fully redacted anonymous resolver. The application adds a noindex owner dashboard with POST-only actions, a GET-only/no-cookie/no-referrer resolver, and a pure disclosure page. It does not capture attribution, consent, touchpoints, referral credit, rewards, commissions, orders, payments, or finance state.
- Independent review caught a P1 terminal-state defect before this checkpoint closed: an owner could directly re-enable a stored `expired` link with a future timestamp. Additive migration 032 now locks the exact owner row and rejects both stored-expired and elapsed-expiry mutations before any update. A hosted rollback rehearsal, permanent apply, full post-apply adversarial probe, and independent code/React/TypeScript/database/Sol security reviews pass with no remaining P0/P1/P2. All referral and finance rows remain zero.
- Migration 033 is live and quarantines the six empty legacy `searches`, `search_intents`, `demand_signals`, `unmet_demand`, `missions`, and `mission_progress` relations without adding an application writer or public function. Migration 034 is live and adds only two empty private definition tables for bounded merchant-supply gaps and merchant-recruitment missions. There is no assignment, progress, attribution, consent, reward, finance, AI, or public writer. Rollback rehearsals, permanent apply, post-apply probe, exact trigger/FK/index/ACL checks, and independent database/security/code reviews pass with no remaining P0/P1/P2.
- Migration 035 is live and replaces the unused empty legacy notification columns with a sanitized in-app envelope and server-owned read state. Public notifications force RLS with one active-self SELECT policy, expose only safe columns and bounded list/mark-read RPCs, preserve Realtime membership, and accept no direct application mutation. The private delivery ledger contains no destination or payload, is excluded from Realtime, has no policy/application grant, and cannot be mutated even under replica role. Rollback rehearsal, permanent apply, full rollback-only hosted probe, exact zero-row/RLS/publication verification, generated-type comparison, and independent database/React follow-up reviews pass. The only residual is a P2 operational Realtime isolation test requiring separate owner, foreign, and suspended authenticated sessions.
- Migration 036 is live and adds fixed-template in-app producers only to the five approved request/order/fulfilment write boundaries. The public wrappers preserve their signatures and authenticated-only grants; six PostgreSQL-owned private functions have no application grant. Source success and notification enqueue are one transaction, replay emits nothing, suspended customers do not veto source transitions, vendor fan-out is deterministic, service-role legacy request DML is revoked, and profile deletion retains hidden immutable recipient-free evidence instead of an unsafe Realtime DELETE. Combined rehearsal, permanent apply, complete post-apply probe, exact zero-residue verification, nullable generated-type reconciliation, and two final independent security reviews pass with no remaining P0/P1/P2. External delivery remains impossible; separate actual WebSocket sessions remain a P2 operational check.
- Migration 037 is live and activates only the smallest truthful unmet-demand evidence boundary: one private market/category/day marker and one authenticated fixed-input writer. It persists no query text, account/profile/session/contact, provider, referral, mission, finance, or AI data. Complete-catalog UI suppression, query-free/no-referrer confirmation, exact auth continuation, current database zero-supply revalidation, statement-snapshot insertion, replay non-oracularity, ACL/RLS isolation, immutability, malformed/rejected-input behavior, supply-activation rejection, and zero fixture residue pass the hosted rehearsal and post-apply probe. Sol security, React, and TypeScript follow-ups pass with no remaining P0/P1 implementation finding. Genuine two-session replay and ingress rate control remain explicit activation checks; the single-session probe does not overclaim concurrency proof.
- The current advisor snapshot reports 49 expected INFO [RLS-no-policy notices](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), one reviewed anonymous-definer WARN, and 38 reviewed WARN [authenticated `SECURITY DEFINER` RPCs](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable). Performance is 64 total: 59 expected INFO [unused indexes](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index), four reviewed [unindexed foreign-key watches](https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys), and one existing WARN for intentional [multiple-permissive request policies](https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies). There are no errors.
- Generated public types contain 51 tables and 78 functions and exclude all 25 private tables, including the demand/referral-mission definitions, notification delivery ledger, and unmet-demand marker. Private function inventory is 44.

## Source implementation present

Guest-first browse/search/detail/auth-intent/email/Google/Facebook routes, the profile/capability model, guided resumable vendor onboarding, deterministic category/ALE and media workflows, the protected listing-request journey, protected order snapshot creation/tracking, the accessible active-market notification inbox, the read-only active-market super-admin triage queue, the bounded non-financial merchant-referral link/disclosure slice, the explicit privacy-bounded zero-supply category confirmation flow, and report-before-AI gating exist in source. `OPENAI_MODEL` is environment-configurable. AI remains **NOT STARTED** and the application makes no OpenAI request.

The request journey covers live-listing intent creation, exact post-auth resumption, explicit confirmation, idempotent request persistence, customer history/detail, a vendor inbox, and secure vendor accept/decline processing with synchronized customer-visible outcomes. Demo listings never persist a request, and request processing does not implicitly create an order.

The separate order journey covers explicit orderability, quantity 1–100, an opaque capability, exact post-auth resumption, server-owned quote revalidation, an explicit placement POST, atomic immutable `placed` order/item/event/audit creation, bounded idempotent replay, read-only customer/vendor history/detail projections, the protected vendor `placed -> confirmed | cancelled` transition, a separate protected `confirmed / not started -> confirmed / processing` fulfilment boundary, and atomic in-app producer notifications for the approved successes. Demo or non-orderable listings never persist an order. Payment collection, inventory mutation, variants, external notification delivery, fulfilment method/address/handoff/completion, and ledger work are not implemented.

Production catalog pages and the sitemap now read the authoritative project through a server-only, cookie-free Supabase publishable-key client and anonymous RLS. Queries redundantly require an active market, active categories/locations, active admin-approved businesses, and active listings with `published_at` set. Mapping is bounded and defensive, route identities are collision-safe, search uses authoritative categories/locations, and provider failure never falls back to fictional records. The intentionally empty live project therefore produces truthful unpublished/empty states.

The operational demand slice is deliberately narrower than the Master Directive's eventual analytics vision. Only an authenticated explicit confirmation for one recognized category in a complete live zero-supply catalog can call the fixed writer. The route carries no search text, uses a no-referrer full navigation, isolates unrelated guest-intent state, repeats every trust check in the Server Action, and returns no existence/count oracle. Quantified trends, raw demand, low-supply heuristics, admin/ATLAS readers, missions, referral attribution, rewards, finance, external sends, and AI remain absent.

The source still contains deterministic guest/demo browse, search, and detail fixtures behind explicit demo mode. These are not legitimate production marketplace data and must not be presented as such.

## Quality evidence

The fresh Git synchronization quality checkpoint passes: TypeScript, lint, formatting, **111 files/649 tests** in 179.45 seconds, a fresh demo production build with 68 generated static pages, the earlier explicit demo-disabled live build with 23 generated static pages, 43 app route patterns, a production dependency audit at zero vulnerabilities, and diff check PASS. Focused demand capture passes 6 files/43 tests, including rendered alert/pending accessibility coverage. Migration 037’s hosted rehearsal and post-apply probe pass with zero residue; independent Sol security, React, and TypeScript follow-ups are PASS with no remaining P0/P1 implementation defect.

## Git synchronization boundary

`origin` is the Founder-specified repository `jimmyarphs2/Lafia-LocalHub`. The fetched `origin/main` is the old Android prototype and has no common ancestor with the independent Next.js/Supabase production history on `codex/localhub-production`. The production branch is the preservation source of truth. Do not merge, rebase, reset, or deploy from `main` until the Founder approves an explicit history-reconciliation strategy.

## Remaining blockers

- Supabase service-role runtime secret for cleanup and any future explicitly approved privileged server route; the current admin triage queue does not use it.
- Hosted Auth site URL/redirect allowlist and Google/Facebook credentials.
- Live market, category, and business data.
- Legitimate hosted zero-supply demand journey; this needs live catalog data and hosted Auth. Genuine two-session replay and production ingress rate controls remain activation checks.
- Listing submission/moderation policy and publication workflow.
- Hosted request E2E remains blocked by the absent service-role runtime secret, hosted Auth configuration, and legitimate live catalog data.
- Fulfilment method/address policy, ready-for-handoff/completion evidence, reversals/disputes, and their eventual payment/settlement interaction require a separate approved contract; current processing state is intentionally narrower.
- Cleanup scheduler/secret and trusted ingress/HMAC configuration.
- Domain, deployment, monitoring, and hosted E2E configuration.
- Payment/legal approvals and referral attribution-window, consent/privacy, retention/deletion, qualification/fraud, and eventual economics decisions.
- Step 24 Paystack activation is externally blocked by Founder/provider/legal/accounting decisions and credentials. Activation additionally requires constant-time verification, identifier PII/retention policy, typed atomic commands/ACL, true two-session concurrency, provider E2E, monitoring, and kill switches.
- Full admin decision/contact/detail/audit-view policy; the current queue is deliberately read-only.
- Explicit pre-AI Founder checkpoint.

## Exact next action

Master Step 27’s minimal operational demand recorder is complete and hosted-verified. Step 28’s first provider-neutral, internal platform-summary boundary now exists in code and contract tests, but migration 038 remains unapplied and is not yet hosted-verified. The next safe action is independent code/security review followed by a rollback-only rehearsal against the authoritative Supabase project. This step must not expose unrestricted database/service-role/provider credentials, invoke OpenAI/Grok, contact users/vendors, publish campaigns, mutate finance, or perform infrastructure actions. Step 29 ATLAS interfaces, Paystack, external provider activation, full admin mutations, fulfilment completion, publication, live data, hosted Auth, deployment, and the explicit pre-AI checkpoint remain separate boundaries.
